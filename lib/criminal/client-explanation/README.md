# Client Explanation Mode (slice 1)

**Status:** Plain-English draft for solicitor review — not legal advice, not plea advice, not sent to client automatically.

**Flags:** `?reasoningV2=1` **and** `?clientExplain=1`. `localStorage: casebrain:clientExplain=true`.

**Not stored:** explanation text, bundle/evidence/client body, paths, IDs.

**Slice 2 (planned):** optional export type in Solicitor Export Builder; client-safe PDF — after solicitor review workflow stable.
