# Synthetic fixtures (tiny)

These fixtures exist only for foundation contracts. They are **not** a real Stage-3000 population and must not be frozen as production membership.

| File | Purpose |
|------|---------|
| `mini-plan.json` | Tiny population plan pin for docs/examples |
| `sample-candidate.json` | Single synthetic candidate shape |

Do not point the unbound production CLI at these as if they were a live corpus.
